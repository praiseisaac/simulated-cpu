New peripheral for CPU

Proximity sensor that triggers when the cursor is close to it. 

To detect the mouse position, you can use document.addEventListener...

Let's also add a peripheral that displays the output of the distance between the cursor and the sensor

In order to fit things on the screen for the program, we have to have it slide across the screen with the information data from the sensor.